
# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Authentication state (contains sensitive session data)
.auth/
*.json

# But we need these files
!package.json
!package-lock.json
!tsconfig.json
